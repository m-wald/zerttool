<h1>Kurs</h1>
<?php
    use Zertifizierungstool\Model\Kurs;
    use Zertifizierungstool\Model\Benutzer_Kurs;
?>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
    
    <div class="container">
    	<h3>Ihre Kurse: </h3>
            <table class="table table-hover">
        
        <tr>
        	<th> KursName </th>
		<th> KursStart </th>
                <th> KursEnde </th>
                <th> KursLeiter </th>
                <th></th>
                <th></th>
		</tr>
		
		<?php 
			foreach($result as $row){ ?>
			<tr>
				<td><?=$row['kurs_name']?></td>
				<td><?=$row['kurs_start']?></td>
                                <td><?=$row['kurs_ende']?></td>
                                <td><?=$row['kurs_benutzername']?></td>					
                                <td> <form accept-charset="utf-8" method="POST" action="kursview">
                                        <input type="hidden" value="<?=$row['kurs_id']?>"name="kurs_id">
                                        <input size="70" maxlength="250" value="anzeigen" type="submit">
                                </form> </td>
                                <td>
                                <?php 
                                 // Prüfen, ob Teilnehmer für den Kurs schon eingetragen ist
                                $signintest = $benutzer_kurs->insert(User::currentUser()->getBenutzername(), $kurs->getKurs_id());
                                // Wenn Benutzer im Kurs eingetragen ist, dann kann er austreten
                                if((User::currentUser()->istTeilnehmer()) && ($signintest == -1)) {
                                    ?>
                                    <form accept-charset="utf-8" method="POST" action="signoutkurs">
                                        <input type="hidden" value="<?=$kurs->getKurs_id()?>" name="kurs_id">
                                        <input size="70" maxlength="250" value="Aus Veranstaltung austragen" type="submit">
                                    </form>
                                    <?php
                                }
                                ?>
                                </td>
			</tr>
		<?php }?>
		
		</table>

    </div>
</body>

